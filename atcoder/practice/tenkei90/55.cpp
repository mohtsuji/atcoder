#include <bits/stdc++.h>
#include <numeric>
#define ll long long

using namespace std;

int main()
{
    int n, p, q;
    cin >> n >> p >> q;
    vector<ll> a(n);
    for (int i=0; i<n; i++) {
        cin >> a[i];
    }

}